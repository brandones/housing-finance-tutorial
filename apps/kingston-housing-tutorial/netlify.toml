# Netlify deploy config for the kingston-housing-tutorial app.
# In the Netlify UI, set the base directory to:
#   apps/kingston-housing-tutorial
# Netlify then reads this file and treats paths below as relative to that base.

[build]
  command = "npm ci && npm run build"
  publish = "dist"
